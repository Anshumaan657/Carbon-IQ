# Public API origin only; never place secrets here.
NEXT_PUBLIC_CARBONIQ_API_URL=http://localhost:8000/api/v1
# Use demo explicitly for the standalone visual demonstration. API failures never fall back to demo.
NEXT_PUBLIC_CARBONIQ_MODE=demo
